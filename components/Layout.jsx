import React from 'react';



export const metadata = {
title: "MessBlock",
description: "Join the decentralized way to comunicate",
};

const Layout = ({ children }) => {
return (
<div>

<div className="main">

</div>
<main className="app">{children}
</main>
</div>
);
} 
export default Layout; 